[pytest]
; Only the dependency-free unit tests run by default. The scripts directly in
; tests/ are manual smoke checks needing live services, and eval/ needs an
; ingested corpus.
testpaths = tests/unit
